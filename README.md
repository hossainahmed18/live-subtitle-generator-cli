# live-subtitle-generator-cli
CLI project to generate live subtitles

pre-requisite:
1. run the command: chmod +x ./ffmpeg/ffmpeg
2. paste ffmpeg folder to target
3. create a output directory under target